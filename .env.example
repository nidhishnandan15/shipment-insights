# Optional. Without this, the app falls back to a curated set of question patterns.
# With this set, the app handles arbitrary plain-English questions via Gemini.
# Free key (no credit card): https://aistudio.google.com/apikey
GOOGLE_API_KEY=
